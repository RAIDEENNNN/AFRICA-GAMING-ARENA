import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Clan Arena | Competitive Clan Command Centre",
  description:
    "A dark esports platform prototype for clan recruitment, match requests, tournaments, clips, rankings, chat, and admin operations.",
  openGraph: {
    title: "Clan Arena",
    description:
      "Built for clans. Made to win. Recruit, challenge, upload clips, manage tournaments, and climb verified rankings.",
  },
  icons: {
    icon: "/favicon.svg",
    shortcut: "/favicon.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
